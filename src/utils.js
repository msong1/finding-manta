export const monthList = [
  'january', 'february', 'march', 'april', 'may', 'june', 'july', 'august', 'september', 'october', 'november', 'december',
];

export const prettyMonth = (month) => `${month[0].toUpperCase() + month.slice(1, 3)}`;
